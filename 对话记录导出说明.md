# 如何保存当前对话记录

## 方法 1：VS Code 内置导出（推荐）

1. 在 VS Code 中，点击左侧 Roo 扩展图标
2. 在对话历史列表中找到当前对话
3. 右键点击 → **"导出对话"**（Export Conversation）
4. 选择保存位置，格式为 Markdown（.md）

## 方法 2：手动复制粘贴

1. 用鼠标从对话开头拖选到结尾
2. `Ctrl+C` 复制
3. 打开记事本或任何文本编辑器
4. `Ctrl+V` 粘贴
5. 保存为 `.md` 或 `.txt` 文件

## 方法 3：使用 VS Code 命令面板

1. `Ctrl+Shift+P` 打开命令面板
2. 输入 `Roo: Export Conversation` 或 `Roo: 导出对话`
3. 选择导出格式和保存位置

## 方法 4：查看对话摘要文件

Roo 会在项目目录下生成对话摘要文件，通常位于：
- `.roo/` 或 `.continue/` 目录下
- 文件名类似 `conversation_*.md`

可以直接从这些文件中复制内容。

---

> **提示**：如果你经常需要保存对话记录，建议在 VS Code 设置中查找 Roo 扩展的"自动导出"相关选项。
