# cash carry 银豹查询 APP — 使用文档

> 版本 1.0.6 | Android | Flutter

---

## 一、APP 简介

本 APP 用于银豹（POSPal）多门店库存查询、补货、顾客预定和订单管理。

- **服务器**: `http://192.168.1.138`（需要电脑运行 WebServer）
- **系统密码**: 首次启动需输入授权码，由管理员提供
- **银豹后台**: `https://beta28.pospal.cn`

---

## 二、首次安装

### 2.1 安装 APK

将 `app-release.apk` 传到手机，点击安装，允许"未知来源"安装。

### 2.2 首次启动

1. 打开 APP，显示欢迎页面
2. **系统授权验证**弹窗 → 输入授权码（联系管理员获取）
3. **操作员姓名**弹窗 → 输入你的姓名（用于补货提交记录）
4. 进入主界面

### 2.3 配置门店

1. 底部点 **配置** Tab
2. 在"门店 1"中填写：
   - 门店名称（如：总店）
   - 门店账号
   - 员工工号
   - 工号密码
3. 点 **登录** 按钮 → 成功后按钮变绿"已登录"
4. 多个门店依次配置登录

### 2.4 配置补货服务器

1. 配置页上方"补货配置"：
   - **补货服务器地址**：`http://192.168.1.138`
   - **供货商列表**：默认可直接用，也可自行添加/删除
2. 顶部标题栏右侧小圆点变 🟢 绿色 = 服务器连接成功
3. 所有配置**自动保存**，无需手动点保存按钮

---

## 三、日常使用

### 3.1 查询库存

1. 底部点 **查询** Tab
2. 输入商品条码，或点 ▣ 按钮扫码
3. 点 **查询多店**
4. 显示：
   - 商品名称
   - 条码
   - 供货商（绿色加粗）
   - 单位
   - 进货价
   - 售价（大号红色字体）
   - 各门店库存数量
5. 登录状态区域显示各门店"已登录/未登录"

### 3.2 补货

1. 在查询结果中，点 **补货** 按钮
   → 自动跳转到补货 Tab，条码和供货商已自动填入
2. 核对/修改：
   - 供货商（下拉选择，可切换）
   - 条码
   - 数量
   - 备注（颜色、货号等）
3. 点击图片区域 → 选择"拍照"或"从相册选择"
4. 进入裁剪页面 → 双指缩放/拖动 → 点右上角 **确认 ✓**
5. 点 **提交补货** → 成功后自动返回查询页

### 3.3 顾客预定

1. 补货 Tab → 顶部点 **顾客预定**
2. 填写：
   - 供货商（选填）
   - 顾客电话
   - 条码（选填）
   - 数量
   - 规格说明
   - 照片
3. 点 **提交预定**

### 3.4 订单查询

1. 补货 Tab → 顶部点 **订单查询**
2. 筛选：
   - 供货商下拉框（可选全部）
   - 搜索框（搜电话、条码、说明）
   - 按时间/数量排序
3. 点击订单缩略图 → 全屏查看（再点关闭）
4. **完结**按钮 → 确认后订单标记完成

---

## 四、操作技巧

### 4.1 滑动切换

| 位置 | 操作 | 效果 |
|------|------|------|
| 主界面任意位置 | 左右滑 | 切换 查询↔补货↔配置 |
| 补货 Tab 栏 | 左右滑 | 切换 日常补货↔顾客预定↔订单查询 |
| 日常补货 | 右滑到底 | 回到查询页 |
| 订单查询 | 左滑到底 | 到配置页 |

### 4.2 服务器状态

- 顶部标题栏 🟢 = 补货服务器在线
- 顶部标题栏 🔴 = 补货服务器断网
- **点击圆点** 手动检测状态

### 4.3 自动登录保持

APP 每 15 分钟自动维持登录状态，后台闲置后重新打开无需重新登录。

### 4.4 登录过期自动恢复

查询时如果提示"登录已失效"，APP 会自动重新登录并重新查询，无需手动操作。

---

## 五、远程控制（管理员）

管理员可通过修改服务器文件来实现：

| 文件 | 作用 | 示例 |
|------|------|------|
| `PIC/password.txt` | 系统密码 | `21771737` |
| `PIC/app_version.txt` | APP 版本号 | `7` |
| `PIC/app-release.apk` | 更新包 | 最新 APK |

### 改密码

直接修改 `PIC/password.txt` 内容 → 重启 WebServer → 所有员工 APP 强制验证新密码。

### 推送更新

1. 修改 `pubspec.yaml`: `version: 1.0.6+6` → `version: 1.0.7+7`
2. 构建新 APK: `flutter build apk --release`
3. 部署：
   ```
   copy app-release.apk → PIC/app-release.apk
   echo 7 > PIC/app_version.txt
   ```
4. 所有已安装 APP 启动时提示更新

---

## 六、常见问题

| 问题 | 解决 |
|------|------|
| 无法连接服务器 | 确认电脑 IP，APP 标题栏圆点变绿才正常 |
| 工号登录失败 | 检查门店账号/工号/密码，先在网页端试一次 |
| 扫码不识别 | 保持 10-20cm 距离，光线充足，对准条码框 |
| 提交补货成功但后台没数据 | 检查供货商是否填写，照片是否上传 |
| APP 提示更新循环 | 服务器 APK 版本号与 `app_version.txt` 不一致 |

---

## 七、版本历史

| 版本 | 日期 | 主要更新 |
|------|------|----------|
| 1.0.6 | 2026-05 | 滑动切换、服务器状态、自动登录保持、心跳机制 |
| 1.0.5 | 2026-05 | 布局调整、一维码图标、提交后跳转 |
| 1.0.4 | 2026-05 | 版本号显示、更新系统完善 |
| 1.0.1 | 2026-05 | 首批功能：查询、补货、配置 |
